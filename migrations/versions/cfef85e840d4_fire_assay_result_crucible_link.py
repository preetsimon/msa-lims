"""fire assay result crucible link

Revision ID: cfef85e840d4
Revises: e4d969607d8c
Create Date: 2026-08-25 02:45:27.360746
"""

from __future__ import annotations

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

revision: str = "cfef85e840d4"
down_revision: str | None = "e4d969607d8c"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    # Schema-only by design: Postgres grants are table-level, so msa_app's
    # existing SELECT/INSERT on fire_assay_result already cover the new
    # column — there is no mutability decision to make here, and no
    # companion grants migration (contrast b1d0c4e77a10's rule for new
    # *tables*, where the decision genuinely has to be made).
    op.add_column("fire_assay_result", sa.Column("crucible_id", sa.BigInteger(), nullable=True))
    op.create_index(
        op.f("ix_fire_assay_result_crucible_id"), "fire_assay_result", ["crucible_id"], unique=False
    )
    op.create_foreign_key(
        op.f("fk_fire_assay_result_crucible_id_crucible"),
        "fire_assay_result",
        "crucible",
        ["crucible_id"],
        ["id"],
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_constraint(
        op.f("fk_fire_assay_result_crucible_id_crucible"), "fire_assay_result", type_="foreignkey"
    )
    op.drop_index(op.f("ix_fire_assay_result_crucible_id"), table_name="fire_assay_result")
    op.drop_column("fire_assay_result", "crucible_id")
    # ### end Alembic commands ###
